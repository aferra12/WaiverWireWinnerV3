fastapi[standard]==0.115.*
uvicorn[standard]==0.34.*
jinja2==3.1.*

numpy==2.2.4
pandas==2.2.3
requests==2.32.3
python-dotenv==1.1.*

# TLS fingerprint impersonation. FanGraphs sits behind Cloudflare, which rejects plain
# `requests` with a 403 challenge no matter what headers are sent -- the handshake itself
# is what gets fingerprinted. curl_cffi replays a real Chrome fingerprint.
curl_cffi==0.15.0
